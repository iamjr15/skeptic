node_modules/
dist/
.next/
/target/
.env
.env.local
*.pyc
__pycache__/
.venv/
venv/

# Tool caches
.mypy_cache/
.ruff_cache/
.pytest_cache/
# Coverage-report output only — anchored so it never matches source/test
# directories named "coverage" (e.g. __tests__/.../coverage).
/coverage/

# OS files
.DS_Store

# Skeptic run bundles, daemon state, and local evidence are never source files.
.skeptic/
skeptic-output/

# Personal agent runtime state
last-session.json
scheduled_tasks.lock

# Internal planning blueprints — kept local, not published (discuss license
# posture / competitive analysis; not appropriate for the public repo).
/docs/v2/blueprint.md
/docs/v2/reference.md
