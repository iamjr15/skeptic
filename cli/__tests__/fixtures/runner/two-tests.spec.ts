import { test } from "skeptic-cli";

test("two-tests: alpha", async () => {
  // Intentionally trivial — exercise the registry + runner without a network hop.
});

test("two-tests: beta", async () => {});
