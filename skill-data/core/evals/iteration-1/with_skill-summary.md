# Core skill evaluation — iteration 1, with skill

All four answers used the installed-contract discovery step and preserved Skeptic's deterministic-tool boundary. The strongest recurring behaviors were fresh snapshot refs after UI changes, explicit evidence paths, completeness-versus-verdict separation, and refusal to promote unavailable collection into a pass.

- Eval 1 covers changed and adjacent checkout paths with console, network, accessibility, screenshot/visual, close, report, and artifact-integrity checks.
- Eval 2 separates a changed-scope Doctor hypothesis from runtime network corroboration and describes the report correlation states.
- Eval 3 explicitly selects a device, keeps refs fresh, requires ADBKeyboard for Unicode, and collects screenshot, recording, gfxinfo, and redacted logcat while stating emulator/performance limitations.
- Eval 4 uses the generated workflow, preserves the same-repository write guard, SARIF/artifact/annotation/summary behavior, and denies implicit project-command authority on fork code.

No product code or skill instruction was changed. Metrics are approximate prompt-evaluation counts; the proposed Skeptic commands were not executed against a target application during this prompt-only evaluation.
